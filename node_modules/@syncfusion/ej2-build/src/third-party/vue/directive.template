import { ComponentBase, EJComponentDecorator, allVue, gh } from '@syncfusion/ej2-vue-base';
import * as Vue3 from 'vue-class-component';
import Vue from 'vue';

export const isExecute: any = gh ? false : true;

let vueImport: any;
if (!isExecute || parseInt(allVue.version) < 3) {
    vueImport = (Vue3 as any).Vue;
} else {
    vueImport = Vue;
}

@EJComponentDecorator({}, isExecute)
export class {{arrayDirectiveClass}}Directive extends vueImport {
    public render(): void {
        return;
    }
    public getTag(): string {
        return 'e-{{arrayDirective}}';
    }
}
export const {{arrayDirectiveClass}}Plugin = {
    name: 'e-{{arrayDirective}}',
    install(Vue: any) {
        Vue.component({{arrayDirectiveClass}}Plugin.name, {{arrayDirectiveClass}}Directive);
    }
}

{{tagComment}}
@EJComponentDecorator({}, isExecute)
export class {{DirectiveClass}}Directive extends vueImport {
    public render(): void {
        return;
    }
    public getTag(): string {
        return 'e-{{Directive}}';
    }
}
export const {{DirectiveClass}}Plugin = {
    name: 'e-{{Directive}}',
    install(Vue: any) {
        Vue.component({{DirectiveClass}}Plugin.name, {{DirectiveClass}}Directive);
    }
}
