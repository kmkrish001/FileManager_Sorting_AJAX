using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Reflection;
using Syncfusion.EJ2.{{namespace}};

namespace Syncfusion.EJ2
{
    public partial class EJ2
    {
        {{comment}}
        public {{builderClassName}} {{className}}{{tModel}}(String id)  {{tClass}}
        {
            var {{obj}} = new {{builderClassName}}();
            {{obj}}.ID = id;
            {{obj}}.Output = this.Output;
            {{obj}}.Context = this.Context;
            this.getContext();
            return {{obj}};           
        }

        public {{builderClassName}} {{className}}{{tModel}}(String id , Syncfusion.EJ2.{{namespace}}.{{className}} Model)  {{tClass}}
        {
            var {{obj}} = new {{builderClassName}}(Model);
            {{obj}}.ID = id;
            {{obj}}.Output = this.Output;
            {{obj}}.Context = this.Context;
            this.getContext();
            return {{obj}};
        }

        public {{builderClassName}} {{className}}{{tModel}}(String id, Syncfusion.EJ2.{{namespace}}.{{className}} Model, IDictionary<string,object> htmlAttr)  {{tClass}}
        {
            var {{obj}} = new {{builderClassName}}(Model);
            {{obj}}.ID = id;
            var attrID = EJSUtil.GetHtmlId(htmlAttr as Dictionary<string, object>);
            if (attrID != null)
            {
                {{obj}}.model.Id = {{obj}}.ID = attrID;
            }
            else
            {
                {{obj}}.model.Id = {{obj}}.ID = id;
            }
            {{obj}}.model.GetType().GetProperty("HtmlAttributes").SetValue({{obj}}.model, htmlAttr, null);
            {{obj}}.Output = this.Output;
            {{obj}}.Context = this.Context;
            this.getContext();
            return  {{obj}};
        }
        
        {{tModelHelper}}
    }
}
